BACKEND 

1 add route in server>routes>file
2 import function to server>routes>file from controllers>file

3 in controllers create the function you just imported
    export const likePost = async (req, res) => {
        const { id } = req.params;
        check valid id
        retrieve post 
        update post likes
        res.json(updatedPost)
    }

FRONT END

1 add api call in api>index
2 export const likePost = (id) => avios.patch(`${url}/${id}/likePost) 
3 go into actions>file
4 create the like actions = 
    export const likePost = (id) => async (dispatch){
        try {

        } catch {

        }
    }

5 reducers > post
6 